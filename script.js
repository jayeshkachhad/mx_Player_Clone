function getRandNum(num) {
    return Math.floor((Math.random() * num) + 1)
}

const vidFormat = {
    id: 1,
    name: "name",
    city: "city",
    pan: "pan"
}

console.log(typeof (vidFormat))

let names = []

